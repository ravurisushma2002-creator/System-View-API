# System-View-API
SysView is a lightweight Node.js REST API that exposes real-time system, OS, CPU, memory, network, and process information using only core Node.js modules and returns structured JSON responses.
